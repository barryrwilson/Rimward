# Issue 168: parked-hull padding exit

The independent natural flight on clean `c1888577a27a59685a454fd09c0f67c780d77613` exceeded the unchanged65s dock observation bound while still in cruise. Preserve its failed result at integration `out/quinn-detour-final/live-veridian-qa1/veridian-public/result.json`. No collision, heat or script error occurred, but berth was not reached. The prior station-detour watchdog repair was independently closed; this was a distinct cruise slowdown.

`rec-22` stayed stationary at `(-147.8829,46.5243,-216.2069)`, radius34.6125u, throughout its observed50.6s span. From world42.3557 to86.3587 the player gained31u toward the actual stage goal `(-5,30,-550)` in44s;86 of88 sampled speeds were below3u/s. Progress of at least1u recurred within3.54s, so the unchanged10s watchdog correctly remained engaged. This demonstrates severe conservative crawling, not a proven infinite deadlock or justified wait for moving traffic.

The shared tangent planner's inside-sphere waypoint originates at the obstacle center. For these poses it points inward even though the player is inside the padded keep radius49.0125u. The unchanged brake correctly rejects approaching motion from inside that radius at speeds of at least1u/s; below1u/s it releases. The inward target and braking therefore produce repeated short throttle/brake pulses.

The bounded fix is private to dock-cruise promoted ship geometry. Only a stationary ship outside physical overlap and within the shared `keep + TANGENT_CLEAR` boundary can use it, and only when the chosen target points inward. It preserves the target's tangential direction and adds equal outward travel, giving a45-degree outward course. The extent derives from the existing keep and player radii. Every other promoted or protected keep sphere can veto the course. Moving ships, moving or stationary asteroids, station/sun routing, braking margins, watchdogs, public state and controls retain their policies.

The existing `TANGENT_CLEAR = 1` constant is exported without changing the shared algorithm. Including its1u annulus matters: an initial candidate stopped correcting at `keep`, repeatedly returned to the inward shared branch between `keep` and `keep + 1`, and later cancelled. That failed candidate remains at `out/cruise-pad-near-candidate/result.json` in the repair worktree. Matching the existing geometric boundary removes the toggle without a route latch or timeout change.

## Constructed actual-owner regression

Run `node --import ./scripts/with-css-stub.mjs scripts/issue-168-cruise-pad-exit-test.mjs`. `STAGE_RUNTIME` selects the tested checkout; `STAGE_OUT` selects ignored evidence output. The small durable fixture records the failed flight's initial player position, velocity, quaternion and parked hull geometry.

This is explicitly constructed, not a saved-world or natural-flight replay. A public approach command activates cruise at a far fixture pose, then the recorded near-body position is installed before the first owner update. A fresh command at that near position would select stage instead. Only the existing `rec-22` trajectory is frozen and excluded from its NPC movement owner. Its actual mesh collider remains; the real player, helm, collision and every other actor owner run normally. The10-ship/90-asteroid populations remain intact. No pose is corrected after setup.

On the same inputs, the unchanged parent exits the padded hull plus one player radius after38s. The boundary-aligned candidate exits after4.5167s, then resumes150.0258u of progress toward the actual stage goal within13.2167s total. Minimum physical clearance is11.4853u, with no contact, heat or death events. AP remains engaged at the end: this is exit/progress evidence, not berth proof. The exact promoted-script results are `out/cruise-pad-promoted-parent/result.json` (expected failure) and `out/issue-168-cruise-pad-exit/result.json` (candidate pass) in the repair worktree.

Independent geometry controls exercise both tangent signs, the transition annulus, blocked outward courses, moving-body exclusions and unchanged normal outside tangents. Existing stage, moving-rock, hold-escape, static-watchdog, burner and cruise checks remain required. Final acceptance requires the combined frozen artifact's checks and one serial natural browser journey under the unchanged observation bound; no result here pre-claims that berth gate.
