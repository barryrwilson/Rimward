# Courier shadowing implementation — #236 / #237

## Authority and scope

On 2026-09-17 the owner explicitly confirmed implementation of the scope in
`docs/Issue236237CourierShadowingDesign.md`, including the proposed persisted
mission/shadow fields, dedicated courier lifecycle, separate capacity and read-only
API projection. Apply the three clarifications in `docs/Issue236237DesignReview.md`.
The frozen design's pending-approval language is superseded by this record.

Base: `dab0eea2` (PR #243 merged). Branch: `codex/issues-236-237-courier`.

Outcome: identify and follow a named courier, gather a basic report, receive fair
warnings and withdraw, then file with the employer for the accepted quote.
Introductory espionage remains intact. #238, global stealth, new keys/gauges/SKUs,
merge and deployment are excluded.

## Stage and owners

Stage: ready for independent QA and natural gameplay verification. Rex implemented
through Claude Code; Clawd coordinates. Independent Quinn review uses Codex and
must name the immutable candidate commit. No independent implementation QA or
natural gameplay success is claimed yet.

## Acceptance and evidence gate

The approved design is the full acceptance contract. Required evidence includes
identity and route clearance; observation, LOS, warning/grace and withdrawal;
strict save normalization and lifecycle; employer-only exactly-once settlement;
old-job regression; build and unchanged boot; natural browser success, warning,
reload, exposure and mixed-career flows with intact traffic and console checks.
Synthetic fixtures must be labeled separately from natural play.

Early implementation risk: ordinary `npc.updateRoute` travels at 0.85 of class
cap and reverses its waypoint within 25 units using class-limited turning.
Measure a real freighter endpoint turn before completing integration: its hull
must remain in the certified 50-unit motion corridor to gather intelligence.
Use the approved mission-local speed cap and normal motion; never teleport it
or weaken the corridor certificate to make a test pass.

Narrow implementation write-set adjustment: `src/systems/solarsystem.js` supplies
session-only planet solid bounds to the courier clearance check. This internal
read-only geometry projection is necessary to check actual moving planets;
it adds no persisted field, public observation, event, or change to planet motion.
Initial bounds must use the real seeded phase, not zero-position mesh defaults,
and subsequent bounds must match the rendered planets. The initial draft added
this file before its rationale was recorded here; independent QA must verify the
limited behavior and offer/materialization/runtime certification separately.

Next: Rex produces a bounded implementation and exact commit with focused test,
build and boot logs. Then perform live gameplay verification and independent
security/regression review; repair findings on a new immutable artifact.

Builder verification: focused courier suite (44 grouped checks), build, and the
unchanged full `npm run test:boot` all exited 0. Raw evidence is in the candidate's
`out/issue-236-237/` (`focused-test.log`, `build.log`, `test-boot.log`, and harness
transcripts). A real-NPC fixture ran 240 simulated seconds, 10 reversals and 7,200
frames, with every hull inside the 50-unit corridor; maximum hull offset was
41.96 units. Seeded-load route certification passed 7/7 authored destinations and
55/101 total tested definitions; omissions were 31 gate, 11 planet and 4 asteroid
clearance failures. These figures are builder fixture evidence, not natural play.

Baseline verification at `dab0eea2`: `npm run test:boot` exited 0; the existing
`scripts/issue-203-205-209-test.mjs` spy-status/employer-return regressions also
passed. Baseline boot output is retained in the everyday checkout at
`out/issue-236-237-baseline/boot.log`. This is baseline evidence only, not a test
of the new implementation. Independent natural probes for success, warning/reload
and exposure are prepared under the candidate's ignored `out/issue-236-237/`;
syntax validation passed, gameplay has not yet run.

## Superseded candidate and QA-fix round

Candidate `55e9244313466e0f2a0244a2d8d5bf6b29bf3027` FAILED independent review
(`out/issue-236-237/independent-review.md`, Codex Quinn) with five blocking
findings, and its natural browser attempt stopped early with a sixth, visual
blocker. That candidate is superseded.

The natural attempt did reach real states before it stopped: ordinary travel and
docking, acceptance of a Freehold to Veridian assignment at a frozen 420 UU,
4.08 observed seconds under normal steering, and a later range loss that
preserved 9.85 seconds. It stopped because the probe controller could not
reacquire the courier from stale target geometry, which is a controller
limitation and not a demonstrated product failure. No completed natural run,
report, payment, warning, reload, exposure or mixed career is claimed.

Repaired in this round, with focused regression pins for each:

1. Recertify the destination before every courier insert or adoption, using all
   actual gate and hub geometry and the real live planet bounds of the current
   destination. A failed certificate ends the accepted assignment as contact
   lost, with no retry, payment or standing.
2. Publish `currentTargetId` only when the exact bound courier meets ordinary
   nearby detection eligibility inside the inclusive 600-unit range, whatever
   selection is retained, and never show a Select instruction beyond it.
3. Reject an out-of-bound saved courier name instead of truncating it; a legal
   40 characters still normalizes exactly as before.
4. Give the human status line the same final-warning distinction and the same
   remaining warned-danger grace the API already published, as text only.
5. Name the unambiguous rendezvous side, in a frame that fixes it: hold off the
   dock upright, world up, facing the first gate, and the rendezvous is 900
   units out to the right. `T = (-D.z, 0, D.x)` is exactly that right-hand
   vector under the engine's forward -Z / up +Y convention; this was checked
   numerically, not assumed, and the API prose states the same reference.
6. Move the persistent mission status off the right-hand panel stack, where it
   was obscured by the agent-play panel at 1440x900.

Evidence is automated only: 51 focused checks, `npm run build` and an unchanged
`npm run test:boot`, all exit 0 (`out/issue-236-237/qa-fix-*.log`), plus the
reviewer's own `security-name-probe.mjs` and `qa-independent.mjs` re-run
unmodified against the fixes. The full boot suite ran immediately BEFORE the
final copy-only change to the human briefing wording, which touches one product
string and its focused pin and nothing the boot suite covers; the focused suite
and the build were re-run after it (`qa-fix-final-*.log`).

The HUD placement is a source change and still needs live QA at supported sizes.
Independent re-review and the natural browser flows remain open. No natural
gameplay success is claimed.

## Warning-priority correction round

Candidate `fd099a66c8802bfe760325b79d8e61e0d4481e43` closed all five original
findings but FAILED independent Codex Quinn re-review
(`out/issue-236-237/independent-review-fd099a66.md`) on one new blocking
finding: `peekShadowStatus` returned the FIRST accepted row, so an earlier
employer's remote travel instruction hid a live local danger warning and its
remaining grace on the single persistent HUD line.

Repaired here on branch `codex/issues-236-237-warning-priority`. The write set
is `peekShadowStatus` in `src/systems/station.js` plus its focused pins. It is
display arbitration of the projections the shared evaluator already publishes:
no per-job API change, no timer, risk, persistence or action behaviour change,
and no new mission state. The order is local danger, local withdrawal, local
observation, local contact work, docked, then remote travel and filed-report
fallbacks; ties keep the job's existing order in `ctx.world.jobs`.

Inside the danger tier the owner approved ACTUAL URGENCY rather than the
warning label, because `stepShadow` exposes an assignment only when BOTH
published thresholds are met:

    estimatedExposureSeconds =
      max((suspicionMax - suspicion) / suspicionGain, remainingGrace)

read from the already-bounded projection. Smallest estimate first, then least
remaining grace, then the original job order. A `final-warning` label therefore
never jumps the queue on its own.

Evidence is automated only: 52 focused checks, `npm run build` and an unchanged
`npm run test:boot`, all exit 0, plus the reviewer's own `qa-independent-fd099`
repro re-run unmodified and now exiting 0 with its per-job API projection
byte-identical. Raw logs are under this worktree's ignored
`out/issue-236-237/warning-priority-*.log`. The new pins drive real station
integration across several employers: remote-first ordering, both
conflicting-threshold cases, equal-exposure least-grace, an acquired remote
report that must not hide a local warning, local observation and contact ahead
of remote, stable ties both ways round, and the purity of the read.

Independent re-review of this artifact, the HUD live layout check and every
natural browser flow remain open. No natural gameplay success is claimed.

## Rollback and limitations

No deployment is authorized. A future deployment must retain the previous artifact
and compatible pre-feature save backup because old readers may discard this
subtype. #234 remains a separate docking concern; do not bypass safety to complete
an acceptance run. Tuning remains provisional until measured in natural play.
