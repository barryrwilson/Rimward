# Wave 104 REP-05 notes

PR1: `src/game/police-cover.js` — local-system `patrol` covering vs pirate/ace the player fights. Gate `standingRead >= 10`. Additive next to ungated `findPirateWork`. CommLine `Patrol covering.` once per visit.

PR2: `src/game/jump.js` `beginJump` refuses dest standing `< -25` before `jumping = true`. Skip unknowables / hollow / independent. CommLine `No passage.` once per dest per visit.

PR3 Digit 9 copy not this wave.

Merge law: `out/w103/rep05/shared-contract.md` still wins.

No live covering screenshot. Prefer probe + boot pins.
