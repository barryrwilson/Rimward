Re-dispatch: recap Beautiful after kill writes.

`applyPlayerKillStanding` calls `applyAbominationStanding(ctx)` after victim −5 and optional +5. Hangar import is one-way. No grafted hangar row → +5 lands. Grafted hangar row → Beautiful stays −10.

Probe pins: `abom.noPlayerGraft.plus5`, `abom.playerGrafted.capHold`. Extra-probe `abom.playerGrafted.capLeak` PASS.
