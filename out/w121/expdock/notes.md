# Wave 121 EXP remaining Unknowables dock / Archive two-way notes

**Verdict:** leftover **CONSUME**. Name: **no remaining Unknowables dock / Archive two-way leftover.** Named serial: **none**.

## Method

- Graph resolve (`omp`): `proceed_unmodeled` (no active workflow). Did **not** `graph_approve` / `graph_propose`.
- Read live `authored-systems.js` `veil` / hush gate / `th_veil`.
- Read `stations/unknowables.js` + `station.js` dispatch, `DETAIL_STATIONS`, Archive desk.
- Read `data-trade.js` tokens / UU / Assembly `archiveFilePrice`.
- Read `state.js` `FACTIONS.unknowables` / `SYSTEMS` / `EPICS`; `contacts.js`; `model-catalog.js`; `shipyard.js`; `save.js` `WORLD_FIELDS`.
- Cite boot `WAVE94` + `out/w94/unk/probe.mjs` (read only).
- Honor: wishlist EXP, Wave 82/93/94, Wave 92 dock brief, EXP brief — cite, do not edit.
- Code wins over wishlist “Unknowables dock still waits.”
- Domain is **data**. Did **not** start Vite or Chrome. Did **not** claim ports.

## Why CONSUME

Wave 94 un-waited the dock. Live code has:

- Authored `veil` / The Veil / faction `unknowables` / station **The Quiet**.
- Dedicated `buildUnknowablesStation` (`UNKNOWABLES_STATION_PATH`). **Not** `DETAIL_STATIONS.unknowables` (D3 holds).
- Archive on Market: own crystal **400**, rival cube **900**, hostile `No sale.`, illegal-origin refuse.
- EXP-02 two-way: buy own at origin, sell rival at the other Archive. Not dead.
- Digit 0 shipyard. No Archive Digit. No new `WORLD_FIELDS` key.

Rejected as invented work (not a named serial): second dock, generated SYSTEM, `DETAIL_STATIONS` row, `archiveFilePrice` Unknowables mirror, persist pending, Digit steal, UU invention.

`archiveFilePrice` remaining Assembly-only is **not** a player-facing hole (`archivePriceAtDesk` prices Unknowables).

## This pack

Markdown only:

- `docs/Exp04RemainingDockDesign.md`
- `out/w121/expdock/**`

No `src/`. No `scripts/`. No `PROGRESS.md`. No wishlist edit. No sibling docs. No `docs/OwnerDecisionsWave121.md`. Did not steal `out/w121/chartlabel/**`, `out/w121/hudrest/**`, `out/w120/**`, `out/w92/unk/**`.

## Reviews

- Security: self-applied auditor + security-review.md. No CRITICAL/HIGH. Re-review after lock: clean.
- Code/design-doc: self-applied code-review.md. No Blocker/Major. Minors accepted (stale wishlist; Assembly-only helper).
- UI: self-applied ui-audit.md. No Blocker/Major. Specified later UI is live Archive; CONSUME adds none.

## Processes

Started none. No Vite. No Chrome.

## Coupling for orchestrator

Do **not** implement. Chart-label sibling owns `galaxychart.js`. This leftover cited `AUTHORED_IDS` `veil` only. HUD/toast/overlay are other workers.
