# Wave 123 remaining FX leftover after named FX slices notes

**Verdict:** leftover **CONSUME**. Name: **no remaining FX leftover.** Named serial: **none**. Muzzle leftover stays **CONSUME**.

## Method

- Graph resolve (`graph-engineering__graph_resolve`): `execute_workflows` (`r-mt93mgh7-74d858a8`). Primary `omp/workflow-research-and-briefing`. Did **not** `graph_approve` / `graph_propose`. Draft workflows non-binding.
- Research-and-briefing required tools (grounded; none override live `src/`):
  - Open Knowledge `memory_brief` / `memory_search`: HUD three-cameras preference; Wave 112 deputize (no extra weapon families). No remaining-FX hole recorded.
  - Projects `search_projects` / `get_project`: `rimward-web-websim` (slug). Next-action overlay leftover is **sibling**, not this pack.
  - CRM `search_contacts` / `search_opportunities`: no WebSim FX opportunity; contact search did not return a remaining-FX owner ticket.
  - Web research: no grounded WebSim remaining-FX source (unrelated third-party “FX” hits). **Code wins.**
- Read live `combat.js` `spawnMuzzle` / `spawnRipple` / `spawnHitFx` / `stampHullMark` / scrape **1858–1860**.
- Read `ship.js` recoil / shake; `song.js` CUES; `npc.js` death burst; `world.js` wreck `aftermath`.
- Read `hull-marks.js` pool 12; `physics.js` IMPACT 8 / 0.35; `save.js` `WORLD_FIELDS`.
- Cite boot WAVE54 / WAVE55 / WAVE59 (WAVE111 FX named log **absent**; live `host.add` still).
- Honor: Fx01 remaining / scrape / muzzle briefs, wishlist Initiative FX — cite, do not edit.
- Code wins over wishlist FX-01 bullet list.
- Domain is **data**. Did **not** start Vite or Chrome. Did **not** claim ports.

## Why CONSUME

Named slices already live:

- FX-01/02/03 first pass (Wave 54): muzzle, bolts, ripple helper, sparks, `playerFire`, shake, song, death burst.
- Recoil + pooled hull scorches (Wave 59).
- Hull-local shield ripple parent (Wave 111).
- Scrape punch `spawnHitFx` on damaging ram (Wave 114).
- Muzzle leftover **CONSUME** (Wave 114). Name **no remaining FX-01 muzzle leftover.**

Wishlist FX-01 bullets are LIVE or skippable/closed. Idea inbox has no open FX IDEA. Flash map and PHY-04 80 u stay skippable. Music/radio stay closed.

Rejected as invented work (not a named serial): hub pip, new Digit, new persist key, UU, SKU, kit mutate, aim-glass gauges, hitscan combat beam, user shaders from save, second incoming-fire live region, muzzle REAL reopen, IMPACT retune, WAVE111 parent steal, scrape steal.

No second unnamed player-facing FX hole.

## This pack

Markdown only:

- `docs/Fx02RemainingFxDesign.md`
- `out/w123/fxrest/**`

No `src/`. No `scripts/`. No `PROGRESS.md`. No wishlist edit. No sibling docs. No `docs/OwnerDecisionsWave123.md`. Did not steal `out/w123/phyrest/**`, `out/w123/astrest/**`, `out/w114/**`, `out/w111/**`, `out/w110/fx01/**`.

## Reviews

- Security: self-applied auditor + security-review.md. No CRITICAL/HIGH. Re-review after lock: clean.
- Code/design-doc: self-applied code-review.md + reviewer persona. No Blocker/Major. Minors accepted (stale wishlist bullets; missing named WAVE111 FX boot log).
- UI: self-applied ui-audit.md. No Blocker/Major. Specified later UI is live muzzle / ripple / scrape; CONSUME adds none.

## Processes

Started none. No Vite. No Chrome. No CDP.

## Coupling for orchestrator

Do **not** implement. Sibling PHY/AST own other leftover packs. WAVE111 parent / scrape / muzzle CONSUME / IMPACT knobs are cite-only. Serial PR1 does not exist. Muzzle CONSUME not reopened as REAL.
