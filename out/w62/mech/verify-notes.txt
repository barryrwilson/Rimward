## Status
CLEAN

## What I tested
- Ran `node --import ./scripts/with-css-stub.mjs out/w62/mech/probe.mjs`. Result: PROBE PASS (18/18).
- Ran `npm run test:boot`. Process exit 1 from the 8 known errors. WAVE62 pin object is all-true, including new mech pins (`mechHook`, `mechIrisHide`, `mechAfter`, `mechHubNoVein`). No WAVE62 HUD FAMILY FAIL line.
- Static review of `src/ui/hud.css` + `src/systems/hud.js`:
  - Rail `top` / `left` / `translate` live only on the shared `.rw-combat-rail` / `.rw-combat-self` / `.rw-combat-target` rules (`top: 57%`, `translate(calc(-100% - 78px), 0)`, `translate(78px, 0)`). The mech hook does not set those properties.
  - No `#hud[data-family="bio"]` hairlines.
  - No `#hud-mech` / `#hud-bio`.
  - No `innerHTML` in `hud.js`.
  - Family tokens remain `'mech' | 'bio'` only (no `live`).
- Live browser (Chrome CDP at 1600×900; Playwright MCP was locked): dismissed title (New Game), picked Freehold Greenhand.
  - Default: `#hud` `data-family="bio"`. Pupil/cilia `display:block`. `::after` still `rw-iris-spin` with 28 px dashed iris inset. Petals 8×13, radius 6/6/2/2, skew matrix. Rails at 57% / 78 px keep-out. FORE/AFT/SCREEN/SHELL/WPN visible.
  - `sessionStorage['rw-hud-family']='mech'` then wait: family flipped to `mech` on wait 2 (~500 ms). Pupil/cilia `display:none`. `::after` is a masked `repeating-conic-gradient` (ticks 35–40 px radius; mask transparent through 28 px = 56 px diameter keep-out). Petals 6×12, radius 0, transform none. MATCH `::before` 3×8 on the existing lamp. Self rail still 57% / 78 px. Child count stayed 17.
  - Clear override: family returned to `bio` on wait 2; iris spin, skewed petals, and living `::after` returned.

## Bugs found

## Environmental issues
- Playwright MCP browser was already in use (`mcp-chrome-7d372dc`). Live check used local Chrome over CDP instead.
- RANGE / MATCH / LEAD words sit on nodes that stay `display:none` / `is-hidden` until in-range / match-speed / lead-draw. They exist in the `#hud` tree; they were not painted in the idle Freehold shot. Not a missing-word bug.

## Evidence
- Screenshots: `out/w62/mech/browser-default-bio.png`, `out/w62/mech/browser-mech.png` (1600×900).
- Logs: `out/w62/mech/probe.log`, `out/w62/mech/boot-test.log`, `out/w62/mech/browser-probe.log`.
- Test output:
  - Probe: `PROBE PASS`
  - WAVE62: `{"independentBio":true,"beautifulBio":true,"builtMech":true,"livingBio":true,"defaultFamily":true,"afterBuilt":true,"overrideMech":true,"overrideOff":true,"blockedBio":true,"noHullWrite":true,"hairSelf":true,"hairTgt":true,"mechHook":true,"mechIrisHide":true,"mechAfter":true,"mechHubNoVein":true}`
  - Live bio: family=bio; irisVisible=true; petals 8×13 skew; rails 57%/78px.
  - Live mech: family=mech at same 17 children; irisVisible=false; ticks outside 56 px keep-out; petals 6×12 no skew; rails 57%/78px.
  - Clear: family=bio; living look returned.
