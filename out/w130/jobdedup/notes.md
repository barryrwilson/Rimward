# Wave 130 Msn04 job-posting identity notes

**Verdict:** leftover **REAL**. Name: **mining identity uniqueness** (commodity + need + reward + origin). Named serial: **PR1**. Not CONSUME. Named serial is **not** none.

## Method

- Graph resolve (`graph-engineering__graph_resolve`): `execute_workflows` (`r-mta66qwo-af66945a`) selected `codex/workflow-drive-artifact-publishing` (score 59.94, coverage 0.12, terms `artifact`/`doc`/`shar`). That stack is Google Drive publish. This wave is **local repo markdown** under `docs/` and `out/w130/jobdedup/`. Did **not** call Drive. Did **not** `graph_approve` / `graph_propose`. External-share gate unused. Owner write-set is local files. Following Drive would violate the worker scope (no writes outside `docs/Msn04JobDedupDesign.md` and `out/w130/jobdedup/**` except verify).
- Census live `src/systems/station.js` `MINING_SLOTS_PER_SYSTEM`, `MINING_ORE_KEYS`, `pickMiningCommodity`, `nextMiningId`, `makeMiningJob`, `syncMiningJobs`, `replaceMiningJob`.
- Census Digit 2 Jobs: `DOCK_KEY_SERVICES`, `renderJobs`, `boardJobs`, `h()` `textContent`, Digit accept by index.
- Census `src/game/save.js` `WORLD_FIELDS` `'jobs'`, `sanitizeOneJob` mining, `extraOfferedFamily` (slot, not commodity), `JOBS_SANITIZE_MAX`.
- Census other families: trade seed, passenger identical titles, explore landmark wrap, hunt/war record bind, espionage dest bind.
- Census `state.js` `COMMODITIES.rawOre.base` 140, `ORE_TYPES` hardness-1 keys, **read only**.
- Census `agent-api.js` **150** unknown — do not add accept cheat.
- Census `overlay-policy.js` **4** never `paused` — cite only.
- Cite sibling MSN-01 replacement live; Msn03 unique-done; inbox MSN/AST ore guidance — do not steal.
- Domain is **data**. Did **not** start Vite or Chrome. Did **not** claim ports. Did **not** run `npm run test:boot`. Did **not** write `src/`. Did **not** edit the wishlist.

## Why REAL (not CONSUME)

Named hole still live:

- `pickMiningCommodity` independent (`station.js` **2238–2242**).
- Live table size **2** → P(twin) **1/2**.
- `syncMiningJobs` fills by count/slot, not commodity (**2293–2314**).
- `nextMiningId` uniquifies ids only (**2244–2263**).
- Sanitize extras are same origin+slot (`save.js` **606–635**), not same commodity.
- Book math `4 * 140 * 1.4 = 784` matches inbox UU.

MSN-01 **replacement** **does** exist. That is **not** duplicate-generation uniqueness. Do **not** CONSUME on replace-exists alone.

## Deputize (not parked)

| Knob | Freeze |
|---|---|
| Identity | commodity at origin (need/pay follow) |
| Slot 1 | exclude sibling live commodity |
| Exhausted | omit second card |
| Offered twins | remint on sync (prefer slot 1) |
| Accepted twins | leave |
| Ids | do not merge |
| Unique four | do not hide |
| Pay | unchanged |
| Scanner | not this pack |
| Families | mining only in PR1 |
| Persist | none |

## Later write-set (do not edit now)

- Prefer `src/systems/station.js` mining helpers only (`pick` / `make` / `sync` / `replace`).
- Do **not** claim `save.js` cap rewrite.
- Do **not** claim `state.js`.
- Do **not** claim `agent-api.js`.
- Do **not** claim `hud.js` / scanner / asteroids.

## Coupling (do not steal)

- Inbox P2 MSN/AST ore-type guidance (later wave).
- AST-02 rich-region find.
- Unique-four replacement / Msn03 unique-done.
- NAV-10 dock approach. TGT-07 combat cycle.
- Optional PR2 trade/passenger/explore identity.
- Agent API observe/act.
- CTL-02 overlay pause (cite only).

## Graph

`resolution_id` `r-mta66qwo-af66945a`. Decision `execute_workflows`. Primary workflow Drive publish is a **false bind** for this local leftover pack. Local write-set completed as the owner assigned.

## Reviews

Security HIGH (XSS title, pay duplication, persist board wipe, Agent accept, prototype keys, overlay pause) **resolved in freeze**. Code Blocker/Major **resolved in freeze** after omit-if-exhausted, replace-path exclusion, bounded pick, mining-only PR1, and no sanitize commodity drop. UI Blocker/Major **resolved as later mint** (live twin rows stay until PR1).

## Re-review

After first-pass freeze: no new HIGH/CRITICAL. MEDIUM accepted-twin double-pay and `Math.random` documented, not expanded. Did not start Vite/Chrome. Did not write `out/w130/jobdedup/verify/**`.
