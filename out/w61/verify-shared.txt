## Status
CLEAN

## What I tested
- Re-read `out/w61/shared-contract.md` after the pass-3 fix. Confirmed the three prior defects are gone.
- Re-checked glance table §1.1 vs `src/systems/hud.js` 849–874 (Lead), MATCH self lamp, RANGE, contacts, edge arrow, `#hud.in-combat` fade.
- Re-checked switch §3.1–3.6 vs `isBeautiful`, `createShipState` `independent`, `ORIGINS`, living mesh, `save.js` snapshot/restore.
- Re-checked §8–§9: later serial PRs, not Wave 61 implementation.
- Re-checked §6–§7: one `#hud` tree, `textContent` only, no new persist keys, session debug only.
- Git path check: `src/` and `docs/` clean. Untracked: `out/w61/shared-contract.md`, `shared-review.md`, `shared-security.md`.
- Reviews `shared-review.md` (pass 3) and `shared-security.md` now match the save.js keep-keys fact.

No browser run. Wave 61 is markdown only. Graph resolve selected `claude/workflow-browser-assisted-work` on a weak “default/only” match (coverage 0.09). Trigger is live web interaction. This task has no app URL and no credentials. Static data check is the correct path.

## Bugs found

## Environmental issues
None. Browser workflow was a false match. No `src/` or `docs/` edits to run.

## Evidence
### Prior bug 1 — Lead hide rule (fixed)
- Contract §1.1 Lead now: hide for mining (`wSpeed === 0`) or no live ship lock; cannon/disruptor on a live on-glass ship show the mark; `|relVel| ≤ LEAD_MIN_SPEED` (~6) keeps the mark on the hull (no TOF offset); off-glass or `leadProj` off NDC hides it.
- Matches `hud.js` 849–874: `showLead = shipTgt && wSpeed > 0`; `relVel.length() > LEAD_MIN_SPEED` only adds the TOF offset.

### Prior bug 2 — hullKind restore drop claim (fixed)
- Contract §3.4 and §7 now: `snapshot()` writes `player: ctx.player` wholesale (`save.js` 170). `restore()` `Object.assign(ctx.player, snap.player)` (359). `sanitizeRestored` heals NaN vitals only; extra keys **keep**.
- HUD must not write `hullKind`. SHP must allowlist `living`|`built`. Matches `save.js`.

### Prior bug 3 — Q2 vs §3.1 default (fixed)
- §10 Q2 now: after debug and `hullKind`, `isBeautiful` then unconditional `return 'bio'`. Unset `hullKind` on any faction is `bio`. Owner call is only Unknowables **purchased** hull `living` vs `built`. Not a silent mech default.

### Glance set (spot re-check)
- Screen/Shell/petals+LOW/CRIT/SPD/WPN/DIST/name/FORE-AFT/RANGE/MATCH/contacts/edge/combat fade still named with current selectors and gates. MATCH remains self lamp only.

### Switch rule
- Origin rejected. `isBeautiful('independent')` is false. Live default `bio` until SHP writes `hullKind: 'built'`. Function in §3.1 matches that.

### PR plan / persist / tree
- §8–§9: “Do not schedule or land these PRs in wave 61.” Pass.
- One `#hud` tree. World strings → `textContent`. No new localStorage or HUD `WORLD_FIELDS`. Session debug only.

### No src/ or docs/ edits
- `git status --short -- src docs` empty.

### Open questions
- Three owner calls remain in §10. Locked decisions stay in §11.
