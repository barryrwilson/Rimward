NAV-03 Wave 84 verifier record
Date: 2026-08-21
Scope: markdown freeze only. No src/wishlist/PROGRESS edits by this verifier.
Worker files: docs/Nav03AutopilotDesign.md, out/w84/nav03/*.md (verify.txt this pass).

STATUS: CLEAN

================================================================
1. Inventory cites vs live files
================================================================

Autosave / WORLD_FIELDS / restore (save.js)
- KEY = 'rimward-save-v1' at save.js:65. Inventory cites line 66 (SLOT_KEYS).
  Behavior is still the same key string. Cite is off-by-one, not a law error.
- WORLD_FIELDS export save.js:75–97. No 'nav'. Matches inventory 75–98.
- snapshot copies listed keys save.js:953. Inventory 948–953 (fn starts 948).
- restore assign WORLD_FIELDS save.js:1160–1162. flags.saveRestored 1159.
- omit hangar/fieldOre/jobs delete 1164–1168.
- unknown currentSystem → 'freehold' is save.js:1171, not inside 1156–1168.
  Behavior matches; line range is short.
- sanitizeRestored starts 1060 (inventory ~1085–1109). Function exists; range stale.
- SAFE_ID save.js:101. RESERVED_IDS 106–110. Match.
- world.nav in src/**/*.js: 0 hits. flags.autopilot: 0 hits.

ship.js MATCH / throttle
- ctx.js:32 "ship.js must not write ctx.input.throttle". Match.
- ship.js header :60 same law. MATCH block :649–706.
- flags.matchSpeed writer ship.js:697–706. Cancel: dock, jumping, !matchLive, throttleHeld.
- steer from input.steerX/Y ship.js:764–766. Collision skip :843 (docked, jumping, dockPressed).
- docked park :879–886 MATCH off. Steer while jumping: yes today (!docked only) :708.

gate.js jumpRequested
- Sole emit: src/systems/gate.js:558–560
  if (inZone && ctx.input.dockPressed) emit('jumpRequested', { to: near.to })
- grep jumpRequested emitters: gate.js emit, jump.js consume, ctx.js list, song.js cue, main.js comment.
- Zone walk :535–557. JUMP.zone from state. Publish inZone/nearTo/nearHub/nearRouteIndex/nearRouteCount :552–556.
- Hub rebuild to: hub.routes[0], routeIndex 0 :469–474.
- KeyG cycle :500–507.

jump.js charge
- beginJump unknown SYSTEMS[to] ignore :71.
- charge JUMP.chargeTime, midpoint at p>=0.5 :152–157.
- midpoint: despawn, targets.current=null, currentSystem=to, relocate+lookAt(0,0,0), zero vel, systemLoaded :79–123.
- junction arrival hub.routes contains origin :102–103.
- same-frame consume events jumpRequested :139–148.
- JUMP table state.js:542–547 zone 60, chargeTime 2.5, arrivalOffset 50, graceSeconds 5, saveOnJump true.

PHY impact
- PHY.IMPACT_MIN_SPEED 8 physics.js:12. PLAYER_RADIUS 2.4 :7. SUN_HEAT_MULT 2.4 :15. SUN_LETHAL_MULT 1.12 :16.
  AVOID_LOOKAHEAD 40 :19. AVOID_GAIN 1.4 :20. GATE_BORE/TUBE 30/2.2 :21–22. Station cyl 32, y −26…33 :8–10.
- combat.js:1687–1689 skip kind==='player' and speed < IMPACT_MIN_SPEED. Contract cites 1689. Match.

NPC avoid
- applyAvoidBias npc.js:590–638, not exported. steerLive 728–734 not exported.
- gateProbeHits exported :459. minerHoldFromStation exported :880.
- sun body r = sunRadius * SUN_HEAT_MULT :641–661.
- flags.combat npc.js:2256–2260.

Tick order main.js:98–127 matches inventory §8. Pause skips system loop :140–143.
Gate before controls: dockPressed is previous-frame. Jump after ship/world.

Other inventory spots
- controls TRACKED 37–44; Digit 0 not in TRACKED. DOCK_KEY_SERVICES shipyard last station.js:174; Digit 0 :5710–5717.
- galaxychart KeyM no preventDefault :21–24, 240–250. Chart docked force close :258. No node click handlers :184–209.
- hud hub clamp cx-44 :1077. .rw-jump center pointer-events none :632–646. Jump box create :742–744.
- hull bands :1520 (>0.5 ok, >0.25 warn, else crit). hullBand event :1523–1525. ctx.js hullBand :227.
- innerHTML on hud.js/galaxychart.js/gate.js/station.js: 0 hits.
- hail stays live hail.js:8–10. title pause systems/title.js:5,57. origins.js:90,117. modelsbrowser.js:8.
- emit spread ctx.js:231–232.

Cite nits (not law errors): autosave 66 vs 65; restore currentSystem 1171; sanitizeRestored start 1060; controls “337–329” reversed.

================================================================
2. No-teleport / persist / writers / MATCH
================================================================

Contract (wins over brief):
- Sole jumpRequested emitter stays gate.js. Payload to is always near.to.
- AP may only latch ctx.autopilot.wantJump. Gate ORs after inZone AND nearTo === NAV-01 next hop AND world.nav.autopilot.
- beginJump still ignores unknown SYSTEMS[to].
- No ship.object.position write except existing jump.js midpoint + collision slide.
- No skip JUMP.chargeTime 2.5. No currentSystem write from AP.
- Command computer src/game/autopilot.js (later). Mesh writer ship.js. AP does not write ctx.input.
- MATCH: refuse engage while flags.matchSpeed. commLine. Do not clear MATCH. No input.throttle write.
- Persist: world.nav.autopilot boolean on NAV-01 record. No second WORLD_FIELDS key. Autosave key unchanged.
- Restore healer always writes autopilot: false (or omit = false). Owner-confirm no auto-resume.
- Cancel / input / listed hazards: autopilot false, keep NAV-01 route.

Live code today has no autopilot.js and no world.nav. Freeze is consistent with live ownership.

================================================================
3. Worker did not write src / wishlist / PROGRESS
================================================================

Listed worker artifacts are markdown under docs/Nav03AutopilotDesign.md and out/w84/nav03/.
src/** has no 'autopilot' / world.nav.
This verifier did not edit src/, wishlist, or PROGRESS.md.

Working tree already has dirty src/, docs/PLAYER-EXPERIENCE-WISHLIST.md, and PROGRESS.md from other waves. Those files are not in the NAV-03 worker file list. Not attributed to this freeze.

================================================================
4. Cross-cut NAV-01 persist allowlist / hopIndex / restore order
================================================================

NAV-01 shared-contract.md §1.2 shape: dest, path, remaining, status.
§1.4 Wave 84 allowlist: dest, path, remaining, status only. Unknown keys drop.
Sibling autopilot dropped until NAV-03 serial extends the allowlist.
Fail-closed: NAV-01 restore never leaves autopilot.engaged === true.

NAV-01 next hop (§2.3): if plotted and path.length >= 2, next = path[1].
NAV-01 does NOT freeze hopIndex. remaining = path.length - 1 when plotted.
Restore mid-route slices path so path[0] === currentSystem.

NAV-03 never names hopIndex (grep 0 in docs/Nav03AutopilotDesign.md and out/w84/nav03/).
It says “NAV-01 next hop” / nextHop. That maps to path[1], not a saved cursor.
NAV-02 does mention hopIndex; that is a sibling issue, not this freeze.

Restore order:
- NAV-01 first: drop autopilot key → omit = false. Cannot leave flying true.
- NAV-03 PR1 after/with NAV-01: extend field + always write false after restore.
- Contract §2.2 step 5: always autopilot: false even if blob said true.
- Omit-key = false, hangar/jobs pattern.
- ctx.autopilot.engaged must not stay true across restore (brief §6).
- Snapshot after restore must see false so stuffed true cannot round-trip.

HIGH conditions from the brief are NOT met:
- NAV-03 does not assume hopIndex that NAV-01 does not freeze.
- Restore order cannot leave autopilot: true if the named healer/allowlist drop runs.

PR1 “may land with NAV-01” is compatible: either drop (no key) or force-false.

================================================================
5. Interrupt table vs wishlist NAV-03
================================================================

Wishlist (PLAYER-EXPERIENCE-WISHLIST.md 742–782) requires:
- click Autopilot flies route through real gates / jump
- cancel or deliberate manual input returns control, keeps route
- interrupt for danger, damage, combat, blocked approach, missing gate, unresolvable
- never silently continue into a lethal hazard
- not immunity
- no teleport
- cancel during approach / gate transit / intermediate system
- restore decision explicit (resume vs manual)

Contract §5 tokens:
cancel, input, combat, hull, sun, impact, missingGate, blocked, dock, pause, hail, arrive, restore.

Map:
- cancel + input (WASD/QE/RF/afterburner/drift/fullStop + steer deadzone 0.65) → wishlist cancel/manual
- combat rising flags.combat
- hull warn/crit hullFrac ≤ 0.5 (live hud.js:1520)
- sun sunHeat/sunKill + lethal-aim abort §7.4
- impact bodyHit kind !== 'player' speed ≥ 8 (same combat scrape gate)
- missingGate
- blocked 10 u / 20 s + avoid hit
- arrive dest system
- restore force-false (explicit no resume)
- cancel still works during jumping (§4.4 / §8.2)

Extras vs wishlist (fail-closed, not missing): dock, pause, hail (Digit 1..n).
Fire/camera/KeyT/KeyV/KeyM/digits do not cancel. D in gate zone is jump verb.

Table is complete for the named wishlist hazards.

================================================================
6. Design vs live residual (named, not freeze bugs)
================================================================

- wantJump is previous-frame latch (gate before AP). Documented. Do not emit from autopilot.js.
- Pause: loop frozen; disengage must be called at paused=true writers. Documented MEDIUM in security-review.
- applyAvoidBias not exported today. PR4 named.
- Live player can still steer during jump; AP freeze holds heading. Intentional change.
- Inventory line numbers will stale (inventory already warns).

================================================================
7. Graph note
================================================================

graph_resolve returned blocked_ambiguous on unrelated Word/sheet/PR workflows
(coverage ~0.1). Assigned parent task is this NAV-03 verify. Continued.

================================================================
End
================================================================
