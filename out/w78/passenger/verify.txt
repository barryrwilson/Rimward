## Status
CLEAN

## What I tested
- `npm run test:boot`. WAVE78 passenger pins all true (`uniqueFour`, `keepMineFh`, `keepTradeFh`, `keepHuntFh`, `keepPassengerFh`, proto/short/need2/bad-sys/commodity/same-dest drops, cap live hunt + PASSENGER_ROOM = `4+8*N+16`, flood heal keeps unique+mine+trade+hunt+passenger, fill two Freehold passenger slots, hunt still two, stuffed dest ignored at pay, complete→replace twice, cargo unchanged, expire no pay, unique ferry `kind:'ferry'` still `done` and kept, passenger tick source has no addCargo/removeCargo/holdUnits, no `ferry-<sys>` allocator, extraHunt kept, no innerHTML, no full-id SAFE_ID, dest bind `otherSystemId`, replace helper, mouse Accept). WAVE78 hunt (`wave78 msn`) pins all true. WAVE71 / WAVE72 / WAVE74 / WAVE76 all true. No WAVE78 PASSENGER FAIL / WAVE78 MSN FAIL.
- Known suite FAILs only: WAVE4 fence, WAVE26 ferry/haul, WAVE35 haul gate. Not passenger bugs.
- Static vs `out/w77/passenger/shared-contract.md` with Wave 78 hunt-cap override from the impl brief:
  - `kind: 'passenger'`; unique `'ferry'` / id `ferry-consignment` untouched.
  - Cap is live hunt-inclusive formula plus passenger room (`4 + MINING+TRADE+HUNT+PASSENGER slots * N + 16`). Not `4+6N` (that would omit hunt). `extraOfferedHunt` still runs before `extraOfferedPassenger`. Last-resort drop never removes honest offered hunt or passenger.
  - Passenger tick (`tickDeliveryJobs` passenger branch) does not call `addCargo` / `removeCargo` / `holdUnits`. No `survivor` on passenger jobs (`commodity` present drops the row). Unique ferry accept still fronts Provisions (`addCargo(..., FERRY_UNITS)`). Unique ferry delivery still `completeJob` → `done` (stamped `job.destSystem` gate). Passenger complete uses `failed` then `replacePassengerJob` splice+push.
  - Accept is origin-only; stamps `payQuoted` from origin `jobPayFor(..., FERRY_REWARD)` (350). Dest-dock pay rebinds `otherSystemId(ctx, origin)`, ignores stuffed `job.destSystem`.
  - `h()` / overlay use `textContent` only. `src/systems/station.js` has no `innerHTML`. Digit 2 titles/details/reward/state regen from templates + allowlisted station name. `boardJobs` hides foreign offered passenger. Mouse Accept `btn(..., acceptJob(job))`.
  - Proto: hyphen tokens + `RESERVED_IDS`; `passenger-__proto__-0` drops. No `SAFE_ID.test(job.id)`.
- Contract: `out/w77/passenger/shared-contract.md` (Wave 77 freeze). Live cap grew by hunt room then passenger room; that matches the Wave 78 worker brief, not the stale `4+6N` line in the Wave 77 formula comment.
- Playwright MCP: locked (`Browser is already in use ... mcp-chrome-7d372dc`). Vite served `http://127.0.0.1:5173/` and `http://localhost:5173/` (HTTP 200). Did not start a second Chrome profile. Digit 2 live pane not shown. [NO BROWSER COVERAGE]
- Flags checked (hunt pin regression, unique ferry splice, cargo helpers on passenger, cap that drops hunt, innerHTML, stuffed dest pay): none tripped.

## Bugs found

## Environmental issues
- Playwright MCP could not open a page: browser profile already in use. Isolated flag is not on the tool schema. Per brief, did not fight a second Chrome profile.
- Live Digit 2 Jobs pane (passenger + hunt + unique ferry cards) was not captured. Boot pins already dock Freehold, press Digit 2, fill two passenger slots, keep two hunt slots, and complete unique ferry to `done`.
- Graph `graph_resolve` returned `codex/workflow-spreadsheet-production` (coverage 0.14, Sheets). That workflow does not apply to this passenger verify. Work followed the parent verifier brief.

## Evidence
- Screenshots: none ([NO BROWSER COVERAGE])
- Logs: out/w78/passenger/logs/boot-pins.txt, out/w78/passenger/logs/boot-test.log
- Test output: out/w78/passenger/logs/boot-pins.txt
  wave78 passenger: all 39 pins true
  wave78 msn (hunt): all 32 pins true
  WAVE71 / WAVE72 / WAVE74 / WAVE76: all true
  unexpected FAIL: none (WAVE4 / WAVE26 / WAVE35 only)
- Cite: src/game/save.js JOBS_SANITIZE_MAX (hunt+passenger rooms), extraOfferedHunt, extraOfferedPassenger, JOB_KINDS includes passenger
- Cite: src/systems/station.js PASSENGER_SLOTS_PER_SYSTEM 2, syncPassengerJobs, acceptJob origin-only + origin FERRY_REWARD stamp, tickDeliveryJobs passenger dest bind + no cargo, replacePassengerJob, unique ferry completeJob done
