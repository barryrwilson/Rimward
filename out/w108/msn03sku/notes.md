# Wave 108 MSN-03 remaining unique SKU — worker notes

Markdown only. No `src/`. No Vite. No Chrome. No blender.

## Leftover
Last-step grants for remaining `EMPLOYER_KEYS`. Live: Freehold `dart`, Red Ledger `auto`, Veridian null, Hollow null. Unique DONE hide already shipped. Chains already shipped.

## Deputize (owner may override)
- Veridian → `auto` (turret). Hollow → `dart` (missile).
- Reason: two live SKUs; dest2 must not copy Freehold dart / Ledger auto; light `canSeat` false so starters do not smash.
- Fail-closed: `grantChainSku` false at parsed step 3 → **+2 UU** in `finishChainStep` only. Helper never writes credits.
- Dart grant patch matches live Freehold: `{ launcher: 'dart' }` (empty rack).
- No third SKU id. No Gilded employer. No new persist key. No `state.js`.

## Serial (named only)
PR1 `jobs-chains.js` `CHAIN_GRANT` rows. Must not steal Digit 0/8/9. Must not write `state.js`.

## Reviews
- Security: self-applied auditor + orchestrator checklist. Fail-UU caller freeze applied.
- Code/design: self-applied reviewer. One Major fixed (credits write location).
- UI: self-applied ui-audit.md on the spec. No Blocker/Major.

## Honor
Did not edit sibling Msn/Bio/Rep/Shp/Owner docs, wishlist, or `PROGRESS.md`. Did not write `docs/OwnerDecisionsWave108.md`.
