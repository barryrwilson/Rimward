# Wave 124 AI-05 starter grace / hostility pacing notes

**Verdict:** leftover **REAL**. Name: **PR1 starter-grace**. Named serial is **not** none. Do **not** freeze CONSUME.

## Method

- Graph resolve (`graph-engineering__graph_resolve`): `execute_workflows` (`r-mt97khm7-d2f56466`) primary `omp/workflow-open-knowledge-recall`. Did **not** `graph_approve` / `graph_propose`. Draft workflows non-binding.
- Open Knowledge: `memory_brief` + `memory_search` (query WebSim / AI-05 / hostility pacing; tag `rimward`). Strongest project hit `decisions/rimward-wave-112-deputized-wishlist-grooming-answers.md` (cite only). No durable note already froze starter grace. Privacy: did not request private notes.
- Read live `src/systems/npc.js` interest / acquire / demand / duel / law zone.
- Read `world.js` Freehold pirates, Illyx, Vane successor, Dresk `alwaysHuntsPlayer`.
- Read `origins.js` origin stamp; `state.js` `JUMP` / `ORIGINS` / `ACES` (read only).
- Read `save.js` death recover, `WORLD_FIELDS`, live-NPC heal.
- Read traffic mix cap; authored Freehold geography; hail toast vs card.
- Honor: wishlist inbox P0 + Initiative AI-04 — cite, do not edit.
- Code wins over “AI-04 already covers pacing.”
- Domain is **data**. Did **not** start Vite or Chrome. Did **not** claim ports. Did **not** run `npm run test:boot`.

## Why REAL

Named AI-04 who-is-hostile already live:

- Traders never hunt (`mayHuntPlayer`).
- Patrols need scratch or standing ≤ −10.
- Pirates keep wave-32 `playerInterestChance` roll.
- Dresk `alwaysHuntsPlayer`.

Hop / new-game grace already live **and is the playtest failure:**

- `JUMP.graceSeconds` **60** (`state.js` **588**).
- Origin pick stamps `jumpGraceUntil` (`origins.js` **120**).
- Ace duel waits on it (`npc.js` **1927–1935**).
- Playtest: pirate ace ~1 minute after origin pick.

Holes still live:

- No origin-keyed extra window (Greenhand 180 vs Marked/ledgerDebt 0).
- Death recover keeps live NPC interest / duel (`save.js` **1139–1180**); no re-stamp.
- Law zone 300 u is pad-only; Freehold rocks ≈ 1041 u; Illyx gate ≈ 796 u from the field (`ENCOUNTER_BUBBLE` 800).

Rejected as invented work: PHY avoid retune, spawn clearance, pirate mix cap, hail cards, encyclopedia, berth freeze, menu digits, hub pip, new persist key, deleting Illyx/Dresk.

## This pack

Markdown only:

- `docs/Ai05StarterGraceDesign.md`
- `out/w124/startergrace/**`

No `src/`. No `scripts/`. No `PROGRESS.md`. No wishlist edit. No sibling docs. No `docs/OwnerDecisionsWave124.md`. Did not steal `out/w124/berthfreeze/**`, `out/w124/menuinput/**`.

## Reviews

- Security: self-applied auditor + security-review.md. No CRITICAL/HIGH after lock. Clamps + fail-closed + no persist death key frozen in contract.
- Code/design-doc: self-applied code-review.md + reviewer persona. No Blocker/Major after lock.
- UI: self-applied ui-audit.md. PR1 no new chrome. No-HUD-pip is correct. No Blocker/Major.

## Processes

Started none. No Vite. No Chrome. No CDP.

## Coupling for orchestrator

Do **not** implement in Wave 124. Later serial **PR1 starter-grace** owns `npc.js` only by default. AI-04 who unchanged. Dresk not cancelled. Contract wins. Graph resolution id `r-mt97khm7-d2f56466`.
